import 'package:flutter/material.dart';

class CreateMyPotScreen extends StatelessWidget {
  const CreateMyPotScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("등록!")),
    );
  }
}